#include <stdio.h>
#include <errno.h>
#include "D:/PRGLIBRARY/SDL2.h"

int main(int argc, char** argv[]){
    // int a = 5;
    // int b = 40;
    // int *prvni = &a;
    // int *druhe = &b;

    // int buffer = *prvni;
    // *prvni = *druhe;
    // *druhe = buffer;
    // printf("%d;%d", *prvni, *druhe);
    // return 0;
    int pole[] = {1,2,3,4,5};
    printf("%s", pole + 1);

}